<!-- Packages -->
import { Props } from '@storybook/addon-docs/blocks'

<!-- Components -->
import { Button }  from '.'

<!-- Config -->
import {
  DROPDOWN_BUTTON_DEFAULTS,
  SMOOTH_SCROLL_BUTTON_DEFAULTS
} from '../../../config'

# Button

`<button>` element with the class `ada-button`.

**https://developer.mozilla.org/docs/Web/HTML/Element/button**

<Preview>
  <Button>
    Button text
  </Button>
</Preview>

<Props components={{ Button }} />

## Usage

<br/>

### Form

<br/>

#### FloorplansSearchForm

- `className='floorplan-search-form-btn'`
- `type='submit'`

<Preview>
  <Button className='floorplan-search-form-btn' type='submit'>
    Search
  </Button>
</Preview>

### Icon

- `data-icon={IconProps}`

<Preview>
  <Button data-icon={{ children: 'arrow_drop_down' }} />

  <Button
    data-icon={{ 'aria-label': 'Scroll to top', children: 'arrow_upward' }}
  />

  <Button data-icon={{ children: 'chevron_left' }} />

  <Button data-icon={{ children: 'chevron_right' }} />

  <Button data-icon={{ 'aria-label': 'Close sidebar', children: 'close' }} />

  <Button data-icon={{ 'aria-label': 'Open sidebar', children: 'menu' }} />

  <Button data-icon={{ 'aria-label': 'Try again', children: 'refresh' }} />

  <Button data-icon={{ 'aria-label': 'Swap order', children: 'swap_vert' }} />
</Preview>

#### Dropdown

- `name=dropdown`

<Preview>
  <Button name={DROPDOWN_BUTTON_DEFAULTS.name}>
    Dropdown
  </Button>
</Preview>

##### FloorplansSearchForm

- `className='floorplan-search-form-btn'`
- `name='dropdown'`

<Preview>
  <Button 
    className='floorplan-search-form-btn'
    name={DROPDOWN_BUTTON_DEFAULTS.name}
  >
    Bathrooms
  </Button>
</Preview>

#### Smooth Scroll

- `name=smooth_scroll`
- `value={{ duration, to }}`

When clicked, this page will scroll to the top of the element selected by the
`value.to` property at a speed of `value.duration` milliseconds.

<Preview>
  <Button name={SMOOTH_SCROLL_BUTTON_DEFAULTS.name}>
    Smooth scroll button
  </Button>
</Preview>

##### Scroll To Top

- `data-icon={ 'aria-label': 'Scroll to top', children: 'arrow_upward' }`
- `name=smooth_scroll`

<Preview>
  <Button
    name='smooth_scroll'
    data-icon={{ 'aria-label': 'Scroll to top', children: 'arrow_upward' }}
  />
</Preview>
