<!-- Packages -->
import { Props } from '@storybook/addon-docs/blocks'

<!-- Components -->
import Option from '.'

# Option

`<option>` element with the class `ada-option`.

**https://developer.mozilla.org/docs/Web/HTML/Element/option**

<Preview>
  <Option />
</Preview>

<Props components={{ Option }} />

## Usage

<br/>

### Disabled

`Option` component where `disabled=true`.

<Preview>
  <Option disabled>Disabled Option</Option>
</Preview>

### Selected

`Option` component where `selected=true`.

<Preview>
  <Option selected>Selected Option</Option>
</Preview>
