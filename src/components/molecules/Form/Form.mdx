<!-- Packages -->
import { Props } from '@storybook/addon-docs/blocks'

<!-- Components -->
import { Form, FloorplanSearchForm } from '.'

# Form

`<form>` element with the class `adm-form`.  

**https://developer.mozilla.org/docs/Web/HTML/Element/form**

<Props components={{ Form, FloorplanSearchForm }} />

## Usage

<br/>

### Components

<br/>

#### FloorplanSearchForm

<Preview>
  <FloorplanSearchForm
    handleFilter={event => console.warn('@todo handleFilter =>', event.target)}
    handleSearch={event => console.warn('@todo handleSearch =>', event.target)}
  />
</Preview>